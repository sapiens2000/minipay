# AGENTS.md — mini-pay 프로젝트 협업 규칙

이 문서는 이 저장소에서 Codex와 협업할 때 지켜야 할 규칙을 정의합니다.
과제 출처: https://github.com/C4-ComeTrue/c4-cometrue-assignment/blob/main/assignments/mini-pay.md

## 프로젝트 개요

간단한 페이 서비스(mini-pay) 서버 개발 과제. 총 7개 Step으로 구성되며,
각 Step 종료 시 PR을 생성해야 함. 핵심 주제는 **트랜잭션/동시성 설계**와
**대용량 로그의 배치·쿼리 성능**.

- Step 1: 계좌 세팅 (메인/적금 계좌, 격리수준, 일일 한도)
- Step 2: 송금 기능 (짧은 트랜잭션, 대량 동시 송금)
- Step 3: 정산 기능 (1/n 정산, 랜덤 정산, 합계 정확성)
- Step 4: 송금 구조 변경 (Pending 상태, 72시간 만료, Remind)
- Step 5: 적금 기능 (정기/자유 적금, 이자 스케줄링)
- Step 6: 송금 내역 관리 (대용량 로그, 쿼리 튜닝)
- Step 7: 성능 테스트 (nGrinder)

## 협업 원칙 — Codex의 역할

**Codex는 설계자/구현자가 아니라 리뷰어 + 개념 설명자 + 디버깅 파트너다.**

이 과제는 "왜 이렇게 설계했는가"를 스스로 답할 수 있어야 의미가 있는 과제이며,
실제 면접에서 설계 근거를 방어할 수 있어야 함. 따라서:

### Codex가 해도 되는 것
- 사용자가 먼저 만든 설계 초안/구현 코드를 리뷰하고, 빠진 케이스나 트레이드오프를 짚어주기
- 개념 설명 (예: Transaction Isolation Level의 종류, MySQL 락 메커니즘, 낙관적/비관적 락 차이 등) — 단, "그래서 어떤 걸 써야 하는지"는 설명하지 않고 사용자가 스스로 판단하게 유도
- 사용자가 막힌 특정 버그/에러에 대한 디버깅 지원
- 완성된 코드에 대한 코드 리뷰
- 사용자가 스스로 정리한 고민 포인트에 대해 소크라테스식 질문으로 검증 (빠진 부분 찔러보기)

### Codex가 하면 안 되는 것
- 먼저 설계안을 제시하는 것 (격리 수준, 락 전략, 테이블 스키마, 정산 로직 등 어떤 결정이든 사용자보다 먼저 답을 내지 않는다)
- 사용자가 직접 고민하기 전에 답을 알려주는 것
- 요청받지 않은 상태에서 코드를 대신 작성하는 것
- "이렇게 하면 됩니다" 식의 완성된 해결책을 바로 제공하는 것

### 진행 방식
1. 사용자가 특정 Step에 대해 스스로 설계 초안(또는 구현)을 먼저 작성해 온다.
2. Codex는 그 결과물을 검토하고, 놓친 케이스·트레이드오프·엣지 케이스를 질문 형태로 짚어준다.
3. 개념이 부족한 부분은 설명하되, 결론(어떤 선택을 할지)은 사용자가 내린다.
4. 구현 중 막히면 디버깅을 돕는다.
5. Step 완료 후 PR 올리기 전, 전체 코드 리뷰를 요청할 수 있다.

## Branch Guide

- Step마다 이전 Step 브랜치를 베이스로 새 브랜치를 생성합니다.
    - 브랜치 이름은 `step0`과 같은 방식으로 짓습니다.
    - PR은 `대표 브랜치 <- Step 브랜치` 방향으로 생성합니다.
- PR은 반드시 Approve를 받은 후 머지합니다. PR을 올린 뒤에는 리뷰를 요청하세요.

## Git Convention

### 포맷

```
type: subject

body
```

#### type

- 하나의 커밋에 여러 타입이 존재하는 경우 상위 우선순위의 타입을 사용한다.
- fix: 버그 픽스
- feat: 새로운 기능 추가
- refactor: 리팩토링 (버그픽스나 기능추가없는 코드변화)
- docs: 문서만 변경
- style: 코드의 의미가 변경 안 되는 경우 (띄어쓰기, 포맷팅, 줄바꿈 등)
- test: 테스트코드 추가/수정
- chore: 빌드 테스트 업데이트, 패키지 매니저를 설정하는 경우 (프로덕션 코드 변경 X)
- wip: 개발 중 

#### subject

- 제목은 50글자를 넘지 않도록 한다.
- 개조식 구문 사용
    - 중요하고 핵심적인 요소만 간추려서 (항목별로 나열하듯이) 표현
- 마지막에 특수문자를 넣지 않는다. (마침표, 느낌표, 물음표 등)

#### body (optional)

- 각 라인은 불릿 리스트로 작성한다.
    - 예시) - AA
- 가능하면 한 줄당 72자를 넘지 않도록 한다.
- 본문의 양에 구애받지 않고 최대한 상세히 작성한다.
- "어떻게"보다는 "무엇을", "왜" 변경했는지 설명한다.

## Additional Requirement

각 Step의 PR은 해당 Step 요구사항뿐 아니라 아래 명세도 반드시 만족해야 합니다.

- 매 Step마다 테스트 코드를 작성합니다.
    - 커버리지 80% 이상을 맞추지 못하면 PR이 제한됩니다.
- Code Smell을 최소화합니다.


## 기타
- 응답은 한국어로.
- 코드 스타일/기술 스택 등 세부 사항은 진행하면서 이 문서에 추가해나갈 것.
