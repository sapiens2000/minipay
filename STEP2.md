# Step2 설계 논의 정리

## 송금 트랜잭션 락 전략 (메인 계좌 ↔ 메인 계좌)

- 결정: 서로 다른 두 유저의 메인 계좌 간 이체이므로, 락 순서를 id 순으로 정렬해 통일.
  (Step1의 메인→적금 인출과 달리 같은 타입 계좌 간 양방향 이동이라 순서를 안 정하면 데드락 여지 있음.)
- 나머지 세부 사항(격리 수준, 비관적/낙관적 락 여부, 대량 동시 송금 시나리오 대응 등)은 미정 — 구현하면서 채울 것.
